FROM mcr.microsoft.com/dotnet/sdk:9.0 as BUILD

COPY SpheneAPI /SpheneAPI
COPY server/SpheneServer/SpheneShared /server/SpheneServer/SpheneShared
COPY server/SpheneServer/SpheneAuthService /server/SpheneServer/SpheneAuthService

# Build SpheneAPI first
WORKDIR /SpheneAPI/SpheneAPI
RUN dotnet build \
        --configuration=Release \
        --output=/SpheneAPI/bin \
        Sphene.API.csproj

WORKDIR /server/SpheneServer/SpheneAuthService/

RUN dotnet publish \
        --configuration=Debug \
        --os=linux \
        --output=/build \
        SpheneAuthService.csproj

FROM mcr.microsoft.com/dotnet/aspnet:9.0

RUN adduser \
        --disabled-password \
        --group \
        --no-create-home \
        --quiet \
        --system \
        sphene

COPY --from=BUILD /build /opt/SpheneAuthService
RUN chown -R sphene:sphene /opt/SpheneAuthService
RUN apt-get update; apt-get install curl -y

USER sphene:sphene
WORKDIR /opt/SpheneAuthService

CMD ["./SpheneAuthService"]
